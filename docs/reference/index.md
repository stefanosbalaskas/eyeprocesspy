# API reference

The `eyeprocesspy` public surface follows the frozen eyeprocess 0.11.1 API ledger and groups functions into a single Python namespace for reproducible research workflows.

## Public API

The generated reference documents the current package exports, signatures, source locations, and docstrings.

[Browse the generated API reference](api.md){ .md-button .md-button--primary }

## How to find the right function

For end-to-end examples, start with [Getting started](../getting-started.md) or the [articles](../articles/index.md). For scientific fidelity and known Python/R differences, use [Parity and validation](../parity-and-validation.md).

The reference is intentionally broad because the frozen parity surface contains **1,182 resolved APIs** spanning import, data contracts, preprocessing, gaze/AOI analysis, pupil workflows, process measurement, IRT, validation, reproducibility, interoperability, and reporting.
